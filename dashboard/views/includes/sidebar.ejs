
<div class="sidebar" data-color="black">

	<div class="logo">
		<a class="simple-text">
			Sugarizer<br> Dashboard
		</a>
	</div>

	<div class="sidebar-wrapper">
		<ul class="nav">
			<%if (module == 'dashboard') { %>
				<li class="active">
			<% } else {%>
				<li>
			<% }%>
				<a href="/dashboard">
					<i class="material-icons">dashboard</i>
					<p data-l10n-id="dashboard"></p>
				</a>
			</li>
			<%if (module == 'users') { %>
				<li class="active">
			<% } else {%>
				<li>
			<% }%>
				<a href="/dashboard/users">
					<i class="custom-icon xo-icon invert"></i>
					<p data-l10n-id="users"></p>
				</a>
			</li>
			<%if (module == 'activities') { %>
				<li class="active">
			<% } else {%>
				<li>
			<% }%>
				<a href="/dashboard/activities">
					<i class="custom-icon activity-icon"></i>
					<p data-l10n-id="activities"></p>
				</a>
			</li>
			<%if (module == 'journals') { %>
				<li class="active">
			<% } else {%>
				<li>
			<% }%>
				<a href="/dashboard/journal">
					<i class="custom-icon journal-icon"></i>
					<p data-l10n-id="journals"></p>
				</a>
			</li>
			<%if (module == 'stats') { %>
				<li class="active">
			<% } else {%>
				<li>
			<% }%>
				<a href="/dashboard/stats">
					<i class="material-icons">timeline</i>
					<p data-l10n-id="stats">Statistics</p>
				</a>
			</li>
		</ul>
	</div>
</div>
